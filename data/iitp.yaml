#project_name: iitp  # also the folder name of the dataset that under data_path folder
#num_gpus: 1               # unused. use args instead
#data_dir: '/opt/data'    # used only if args.data_dir == ''
train: ../iitpdata/images/train/
val: ../iitpdata/images/val/
#test: ../../iitpdata/lables/val.txt
# mean and std in RGB order
#mean: [ 0.485, 0.456, 0.406 ]
#std: [ 0.229, 0.224, 0.225 ]
nc: 1
# anchors
#anchors_scales: '[2 ** 0, 2 ** (1.0 / 3.0), 2 ** (2.0 / 3.0)]'
#anchors_ratios: '[(1.0, 1.0), (1.4, 0.7), (0.7, 1.4)]'

names: [ 'human' ]

